export const routes = {
  root: '/api/v1',
  categories: {
    create: '/categories',
    receiveMany: '/categories',
    receiveOne: '/categories/id',
    update: '/categories/:id',
    delete: '/categories/:id',
  },
};
